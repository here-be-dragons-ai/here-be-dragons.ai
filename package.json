{
  "private": true,
  "scripts": {
    "build": "echo \"No build step required\"",
    "check": "node --check main.js && node --check api/contact.js && node --check api/contact.test.js",
    "test": "node --test"
  }
}
